{
  "name": "equaledge-ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "start": "wrangler dev",
    "test": "vitest",
    "build:vectors": "node rebuild_vectors.js"
  },
  "engines": {
    "node": ">=18.17.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "vitest": "^3.2.0",
    "wrangler": "^4.79.0"
  }
}
